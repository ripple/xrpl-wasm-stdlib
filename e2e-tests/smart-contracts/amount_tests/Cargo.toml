[package]
name = "amount_tests"
version = "0.1.0"
edition = "2024"
description = "Tests for all Amount variants: XRP, IOU, MPT"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
xrpl-contract-stdlib = { path = "../../../xrpl-contract-stdlib" }
